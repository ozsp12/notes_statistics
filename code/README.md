# Code

Place reusable scripts here. Prefer one subdirectory per language or computational topic when the repository grows, for example `python/`, `r/`, or `simulations/`.

Scripts that generate figures should write their final outputs to `assets/figures/` and document their input data.
